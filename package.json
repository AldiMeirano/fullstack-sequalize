{
  "name": "squalize---copy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && node server\"",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
